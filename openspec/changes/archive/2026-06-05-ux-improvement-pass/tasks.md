## 1. 生命徵象條（live-vitals-bar）

- [x] 1.1 新增常駐 vitals 條 DOM（建議 `#fw` 底部一列）：UPH、已完成/目標、預估完成、瓶頸徽章
- [x] 1.2 樣式：單列、tabular-nums、極輕量、深淺主題相容；不擠壓流程圖
- [x] 1.3 `updateProgressAndStats()` / `updateUI()` 同步刷新 vitals 條（與 `#sp` 共用資料來源）
- [x] 1.4 瓶頸徽章：🟢無瓶頸 / 🔴N 站堆積；點擊展開統計圖表覆蓋層看細節（連動 `toggleBot`）
- [x] 1.5 圖表覆蓋層內的 `#sp`（即時統計/瓶頸細節）保留為「細節版」，避免資料重複矛盾
- [x] 1.6 驗證：圖表收合時仍能看到即時 UPH 與瓶頸；重置/切換線正確歸零

## 2. 控制列分層（command-bar-hierarchy）

- [x] 2.1 將 `#hdr-ctrl` 重排為三群：主要（播放·速度·立即結果）/ 分析（看板·比較）/ 工具（視角·參數·主題·說明）
- [x] 2.2 工具群降權（icon-only / dim 色），主要動作維持最高視覺權重
- [x] 2.3 決定「重置 / 切換線」歸群（依 design Open Question）
- [x] 2.4 驗證 a11y：群組 `role="group"` 與 aria-label 正確、Tab 次序合理

## 3. RWD 斷點 + 簡報模式（presentation-mode）

- [x] 3.1 加入版面斷點 `@media (max-width: …)`：窄視窗控制列換行/收納次要群、縮短 label，不破版
- [x] 3.2 新增簡報模式全域 class（如 `body.present`）：隱藏參數抽屜與次要控制、放大流程圖
- [x] 3.3 簡報模式 vitals 改超大字版（完成/UPH/ETA/瓶頸）
- [x] 3.4 切換入口：header 一顆鈕 + 快捷鍵（建議 `F`），`Esc` 退出；避免與既有快捷鍵衝突
- [x] 3.5 （選用）以 localStorage 記憶簡報模式偏好 — **決定不持久化**：簡報為暫態 demo 狀態，避免下次開啟意外進入
- [x] 3.6 驗證：窄視窗不破版；簡報模式進出正常、流程圖縮放/平移仍可用

## 4. 決策動線抬升（scenario-comparison-table 強化）

- [x] 4.1 達標結果視窗強化「📌 釘選此方案」「📋 開比較表」CTA 呈現
- [x] 4.2 瓶頸出現時提供「查看瓶頸建議」入口（vitals 徽章 / 結果視窗）連到 `bottleneck-advisor`
- [x] 4.3 首次達標時的輕量引導（提示可釘選比較），避免使用者沒發現比較功能
- [x] 4.4 驗證：釘選→比較動線順暢、最佳值標示仍正確

## 5. 視覺一致性 + 可讀性（display-refinements 強化）

- [x] 5.1 流程圖圖例（`abf-hdr2` 等）改易讀排版（分段/加大/或固定角落卡片）
- [x] 5.2 站別狀態加形狀/標記編碼（滿框/空框/加工中），去色仍可分辨（色盲友善）— 已存在：`SLOT_COLORS` 各狀態帶 `sym`（●滿/○空/▶加工/◌預訂），與圖例符號一致
- [x] 5.3 統一圖示風格、盤點關鍵數值字級與 WCAG AA 對比，必要處上調
- [x] 5.4 驗證：淺/深主題、去色（灰階）下狀態皆可分辨

## 6. 成績單式結果 + 空狀態（result-scorecard）

- [x] 6.1 達標/計算結果視窗重做為成績單版面（產能·瓶頸·ETA 一眼到位 + CTA）
- [x] 6.2 空狀態引導：尚未開始模擬、尚無結果可匯出/釘選時的提示
- [x] 6.3 驗證：達標與「達上限未達標」兩種結果皆正確呈現

## 7. 整合驗證（含桌面 WebView2）

- [x] 7.1 ABF 線完整跑：vitals、控制列、簡報模式、比較動線、結果視窗皆正常
- [x] 7.2 SMK 線重複驗證
- [x] 7.3 桌面版（Tauri WebView2）以 CDP 實機驗證：選擇畫面正常載入、版面不破、簡報模式進出、圖表覆蓋層仍可開關/縮放
- [x] 7.4 確認既有模擬數值（產出/UPH/瓶頸/折線圖/匯出）不受影響
- [x] 7.5 視窗縮放（窄→寬）與深/淺主題交叉驗證
